[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "pydantic-settings",
    "httpx[socks]",
    "pytest",
    "python-multipart",
    "pillow",
    "openai",
    "langchain",
    "langgraph",
    "paddlepaddle==3.2.0",
    "paddleocr",
    "paddlex[ocr]",
    "typer",
]

[tool.pytest.ini_options]
pythonpath = ["."]
