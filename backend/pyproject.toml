[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "pydantic-settings",
    "httpx",
    "pytest",
    "python-multipart",
    "pillow",
    "openai",
    "langchain",
    "langgraph",
    "paddlex",
]

[tool.pytest.ini_options]
pythonpath = ["."]
